# Xử lý PDF cơ bản (merge, split, remove bảo mật...)
PyPDF2>=3.0.0

# Chuyển PDF → DOCX (văn phòng có định dạng)
pdf2docx>=0.8.0  # hỗ trợ Converter/parse :contentReference[oaicite:1]{index=1}

# Chuyển DOCX → PDF (Windows/macOS qua Office)
docx2pdf>=0.1.9  # yêu cầu MS Office :contentReference[oaicite:2]{index=2}

# Tạo PDF đơn giản từ scratch
fpdf>=1.7.2     # nhẹ, tích hợp HTMLMixin hỗ trợ HTML cơ bản :contentReference[oaicite:3]{index=3}

# Tạo PDF chuyên nghiệp, báo cáo, đồ họa
reportlab>=4.0.0 :contentReference[oaicite:4]{index=4}

# Chuyển HTML → PDF bằng wkhtmltopdf
pdfkit>=1.0.0   # wrapper cho wkhtmltopdf
py3-wkhtmltopdf>=0.4.1 :contentReference[oaicite:5]{index=5}

# Chuyển HTML → PDF bằng headless browser
pyppeteer>=1.0.0 :contentReference[oaicite:6]{index=6}
playwright>=1.35.0  # cross‑browser support :contentReference[oaicite:7]{index=7}

# Xử lý ảnh → PDF (lossless)
img2pdf>=0.4.0
Pillow>=9.5.0     # hỗ trợ xử lý ảnh trước khi convert :contentReference[oaicite:8]{index=8}

# Chuyển PDF → CSV/Excel/table
tabula-py>=2.7.0  # trích datasheet từ PDF :contentReference[oaicite:9]{index=9}

# Nếu cần đọc PDF dạng bảo mật / nâng cao annotation, forms
borb>=2.6.0       # generate forms, bảng, barcodes :contentReference[oaicite:10]{index=10}

# Xử lý môi trường và logging
python-dotenv>=1.1.1  # load biến môi trường
loguru>=0.7.0         # logging đẹp, tiện dụng
